{
  "name": "mealloop-monorepo",
  "version": "1.0.0",
  "description": "MealLoop Food Donation Platform",
  "scripts": {
    "build": "cd frontend/mealloop && npm install && npm run build",
    "build:frontend": "cd frontend/mealloop && npm install && npm run build",
    "build:backend": "cd backend && npm install",
    "start": "cd backend && npm start",
    "dev:frontend": "cd frontend/mealloop && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend/mealloop && npm install",
    "postinstall": "echo 'Root dependencies installed. Run npm run install:all to install all project dependencies.'"
  },
  "workspaces": [
    "backend",
    "frontend/mealloop"
  ],
  "dependencies": {
    "dotenv": "^17.2.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=8.0.0"
  }
}
